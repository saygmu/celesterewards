name = "celesterewards-sync"
main = "worker.js"
compatibility_date = "2024-12-01"

# KV namespace: 部署前要先建：
#   npx wrangler kv namespace create STATE_KV
# 然後把回傳的 id 填到下面：
[[kv_namespaces]]
binding = "STATE_KV"
id = "REPLACE_WITH_KV_ID"
